import { LogoutButton } from "@/components/container/LogoutButton";
import { PageContainer } from "@/components/container/PageContainer";
import { Button } from "@/components/ui/button";
import { motion } from "framer-motion";
import { 
  Play, 
  Settings, 
  Trophy, 
  Users, 
  Gamepad2
} from "lucide-react";

export default function ExamPage() {
  const menuItems = [
    { 
      id: 'play', 
      label: 'START GAME', 
      icon: Play
    },
    { 
      id: 'multiplayer', 
      label: 'MULTIPLAYER', 
      icon: Users
    },
    { 
      id: 'leaderboard', 
      label: 'LEADERBOARD', 
      icon: Trophy
    },
    { 
      id: 'settings', 
      label: 'SETTINGS', 
      icon: Settings
    }
  ];

  return (
    <PageContainer>
      <motion.div 
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ duration: 0.5 }}
        className="min-h-screen bg-background relative"
      >
        {/* Header */}
        <motion.header 
          initial={{ y: -50, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ duration: 0.6, ease: "easeOut" }}
          className="flex justify-between items-center p-6"
        >
          <motion.div 
            whileHover={{ rotate: 15 }}
            className="w-12 h-12 bg-main-foreground flex items-center justify-center border-4 border-border shadow-shadow"
          >
            <Gamepad2 className="w-6 h-6 text-main" />
          </motion.div>

          <div className="flex items-center gap-4">
            <div className="hidden md:flex gap-6">
              <div className="text-center">
                <div className="text-2xl font-heading text-foreground">42</div>
                <div className="text-sm font-base text-foreground/70">LEVEL</div>
              </div>
              <div className="text-center">
                <div className="text-2xl font-heading text-foreground">156</div>
                <div className="text-sm font-base text-foreground/70">WINS</div>
              </div>
            </div>
            <LogoutButton />
          </div>
        </motion.header>

        {/* Main Content */}
        <div className="flex-1 flex items-center justify-center px-6 py-12">
          <div className="max-w-2xl w-full">
            {/* Title Section */}
            <motion.div 
              initial={{ scale: 0.9, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              transition={{ duration: 0.8, ease: "easeOut" }}
              className="text-center mb-12"
            >
              <motion.h1 
                initial={{ y: 30 }}
                animate={{ y: 0 }}
                transition={{ duration: 0.6, delay: 0.2 }}
                className="text-6xl md:text-7xl font-heading text-foreground mb-6 tracking-tight"
              >
                GAME ARENA
              </motion.h1>
              <motion.p 
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ duration: 0.6, delay: 0.4 }}
                className="text-xl font-base text-foreground/80 max-w-md mx-auto"
              >
                Ready to play?
              </motion.p>
            </motion.div>

            {/* Menu Grid */}
            <motion.div 
              initial={{ opacity: 0, y: 40 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.3 }}
              className="grid grid-cols-1 md:grid-cols-2 gap-4 max-w-lg mx-auto"
            >
              {menuItems.map((item, index) => {
                const Icon = item.icon;
                return (
                  <motion.button
                    key={item.id}
                    initial={{ opacity: 0, y: 20 }}
                    animate={{ opacity: 1, y: 0 }}
                    transition={{ duration: 0.4, delay: 0.5 + index * 0.1 }}
                    whileHover={{ 
                      y: -2,
                      boxShadow: "0px 6px 0px 0px var(--border)"
                    }}
                    whileTap={{ 
                      scale: 0.98,
                      y: 0,
                      boxShadow: "0px 2px 0px 0px var(--border)"
                    }}
                    className="bg-main border-4 border-border shadow-shadow p-6 transition-all duration-150 group"
                  >
                    <div className="flex items-center justify-center gap-3">
                      <Icon className="w-6 h-6 text-main-foreground" />
                      <span className="text-lg font-heading text-main-foreground">
                        {item.label}
                      </span>
                    </div>
                  </motion.button>
                );
              })}
            </motion.div>
          </div>
        </div>
      </motion.div>
    </PageContainer>
  );
}